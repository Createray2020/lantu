# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Cowork 暫存
_to_delete/
*.tar.gz
lantu_src_v1.tgz

# Cowork 暫存打包檔（改法：一律忽略）
*.tgz
_stage*
_incoming*

# ⚠️ 資料庫 dump／備份：裡面是真實客戶的完整財務資料，而這個 repo 是 public。
# 一次 git add -A 就會永久推上 GitHub（git 歷史刪不乾淨），所以一律擋在源頭。
_backup_*
lantu_backup_*.json
*_dump.json
mock_backup_*.json
